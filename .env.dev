# =============================================================================
# SIGA-LACEN — Variáveis de Ambiente — DESENVOLVIMENTO
# Para usar: cp .env.dev .env && docker compose down && docker compose up -d
# NUNCA versione este arquivo.
# =============================================================================

# Django
SECRET_KEY=django-insecure-dev-key-siga-lacen-2026-test-only
DEBUG=True
ALLOWED_HOSTS=siga.lacen.local,localhost,127.0.0.1

# CORS
CORS_ALLOWED_ORIGINS=https://siga.lacen.local

# Banco de dados PostgreSQL
DB_NAME=siga_lacen
DB_USER=siga_user
DB_PASSWORD=siga_dev_pass_2026
DB_HOST=db
DB_PORT=5432

# Django settings module
DJANGO_SETTINGS_MODULE=config.settings.development

# GAL WebService
GAL_WS_USUARIO=
GAL_WS_SENHA=
GAL_WS_URL=https://gal.riograndedosul.sus.gov.br/webservice/automacao
GAL_WS_VERIFY_SSL=False
